<div class="navbar-fixed">
  <nav>
    <div class="nav-wrapper">
      <%= link_to t("static_page.app_name"), root_path, class: "brand-logo" %>
      <ul class="right hide-on-med-and-down">
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <li>
              <a class="dropdown-button btn" href="#" data-activates="dropdown1">
                <%= t "static_page.actions" %>
              </a>
              <ul id="dropdown1" class="dropdown-content">
                <li><%= link_to t("imports.import"), admin_imports_path %></li>
                <li><%= link_to t("semesters.semester"), admin_semesters_path %></li>
                <li><%= link_to t("courses.course"), admin_courses_path %></li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to t("class_rooms.class_room"), class_rooms_path %></li>
          <% end %>
          <li>
            <a class="dropdown-button" href="#" data-activates="dropdown">
              <%= current_user.email %>
            </a>
            <ul id="dropdown" class="dropdown-content">
              <li><%= link_to t("users.views.menu.profile"), "#" %></li>
              <li><%= link_to t("users.views.menu.setting"),
                edit_user_registration_path %>
              </li>
              <li class="divider"></li>
              <li><%= link_to t("users.title.sign_out"), destroy_user_session_path,
                method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("users.title.sign_in"), new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </nav>
</div>
