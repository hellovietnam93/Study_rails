$("#error_explanation").remove()
$("iframe").contents().find("body").empty();
<% if @assignment.errors.any? %>
  $(".edit_assignment #errors-create-assignment").
    html("<%= j render "shared/errors_messages", object: @assignment %>");
<% else %>
  $("#edit-assignment-modal").closeModal();
  $("#assignments .tbody").html("<%= j render @class_room.assignments %>");
  $("#assignments .show-modal").html("<%= j render 'assignments/show_assignment_modal' %>");
<% end %>
set_timetout()
